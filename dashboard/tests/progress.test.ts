import { describe, expect, it } from "vitest";import{curriculum}from"../lib/curriculum";import{completionPercent,defaultState,exportState,importState,lessonForDate,lessonState,searchLessons,type AppState}from"../lib/progress";
const complete=(ids:string[]):AppState=>({...defaultState,progress:Object.fromEntries(ids.map(id=>[id,{lessonId:id,state:"completed" as const,startedAt:"2026-08-07T00:00:00Z",completedAt:"2026-08-07T01:00:00Z",timeSpentMinutes:90,score:80,confidence:4,checklist:[],review:false}]))});
describe("progress",()=>{it("excludes rest days",()=>{expect(completionPercent(complete(curriculum.filter(l=>!l.required).map(l=>l.id)),curriculum)).toBe(0);expect(completionPercent(complete(curriculum.filter(l=>l.required).map(l=>l.id)),curriculum)).toBe(100)});it("unlocks sequential lessons",()=>{expect(lessonState(curriculum[0],defaultState)).toBe("available");expect(lessonState(curriculum[1],defaultState)).toBe("locked");expect(lessonState(curriculum[1],complete([curriculum[0].id]))).toBe("available")});it("calculates the calendar day",()=>expect(lessonForDate("2026-08-07",new Date("2026-08-13T12:00:00"))).toBe(7));it("round trips valid exports and rejects invalid imports",()=>{const state=complete([curriculum[0].id]);expect(importState(exportState(state))).toEqual(state);expect(()=>importState('{"version":99}')).toThrow()});it("searches and filters",()=>{expect(searchLessons(curriculum,"data types",{},defaultState).length).toBeGreaterThan(0);expect(searchLessons(curriculum,"",{phase:"Existing Foundations / Bridge",track:"Python & domain models"},defaultState)).toHaveLength(3)});it("preserves notes in export",()=>{const state={...defaultState,notes:[{id:"n1",lessonId:curriculum[0].id,text:"My explanation",confusing:false,updatedAt:"2026-08-07T00:00:00Z"}]};expect(importState(exportState(state)).notes[0].text).toBe("My explanation")})});
